<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>

        <!-- Modificar la ubicación del mensaje de error. -->
        <record id="transaction_form" model="ir.ui.view">
            <field name="name">payment.transaction.form</field>
            <field name="model">payment.transaction</field>
            <field name="inherit_id" ref="payment.transaction_form"/>
            <field name="arch" type="xml">
                <xpath expr='//field[@name="state_message"]' position='attributes'>
                    <attribute name="attrs">{'invisible': True}</attribute>
                </xpath>
            </field>
        </record>

        <record id="acquirer_form_stripe" model="ir.ui.view">
            <field name="name">acquirer.form.stripe</field>
            <field name="model">payment.acquirer</field>
            <field name="inherit_id" ref="payment.acquirer_form"/>
            <field name="arch" type="xml">

                <xpath expr='//group[@name="acquirer_display"]' position='after'>
                    <group attrs="{'invisible': [('provider', '!=', 'stripe')]}">
                        <field name="stripe_merchant_url" attrs="{'required': [('provider', '=', 'stripe')]}"/>
                        <field name="stripe_merchant_titular" attrs="{'required': [('provider', '=', 'stripe')]}"/>
                        <field name="stripe_merchant_name" attrs="{'required': [('provider', '=', 'stripe')]}"/>
                        <field name="stripe_secret_key" attrs="{'required': [('provider', '=', 'stripe')]}"/>
                        <field name="stripe_url_reply" attrs="{'required': [('provider', '=', 'stripe')]}"/>
                        <field name="stripe_template" attrs="{'required': [('provider', '=', 'stripe')]}"/>

                    </group>
                    <group attrs="{'invisible': [('provider', '!=', 'stripe')]}">
                        <field name="send_quotation"/>
                    </group>
                </xpath>
            </field>
        </record>


        <record id="transaction_form_stripe" model="ir.ui.view">
            <field name="name">acquirer.transaction.form.stripe</field>
            <field name="model">payment.transaction</field>
            <field name="inherit_id" ref="payment.transaction_form"/>
            <field name="arch" type="xml">
                <xpath expr='//notebook' position='inside'>
                    <page string="Stripe TX Details">
                        <group>
                            <field name="stripe_txnid"/>
                            <field name="state_message"/>
                        </group>
                    </page>
                </xpath>
            </field>
        </record>

        <record id="acquirer_sequence" model="ir.ui.view">
            <field name="name">payment.acquirer.list</field>
            <field name="model">payment.acquirer</field>
            <field name="inherit_id" ref="payment.acquirer_list"/>
            <field name="arch" type="xml">
                <xpath expr='//tree' position='inside'>
                    <field name="sequence"/>
                </xpath>
            </field>
        </record>

        <record id="acquirer_form" model="ir.ui.view">
            <field name="name">payment.acquirer.form</field>
            <field name="model">payment.acquirer</field>
            <field name="inherit_id" ref="payment.acquirer_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='website_published']" position="before">
                    <field name="sequence"/>
                </xpath>
            </field>
        </record>



    </data>
</openerp>
